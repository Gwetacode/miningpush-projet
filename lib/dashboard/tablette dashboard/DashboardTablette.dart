import 'package:flutter/material.dart';
class dashboardmenutablet extends StatelessWidget {
  const dashboardmenutablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       appBar: AppBar(
        title: Text("tablette"),
      ),
    );
  }
}